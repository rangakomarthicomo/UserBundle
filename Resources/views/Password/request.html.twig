{% trans_default_domain 'NedwaveUserBundle' %}

{% extends "::base.html.twig" %}

{% block title %}NedwaveUserBundle:Password:request{% endblock %}

{% block body %}
  <h2>{{ 'password.request.title'|trans }}</h2>
  
  {% for message in app.session.flashbag.get('notice') %}
      <div class="flash-notice">
          {{ message|trans }}
      </div>
  {% endfor %}
  
  {{ form_start(form) }}
      {{ form_errors(form) }}
      
      {{ form_row(form.email) }}
      
      <input type="submit" value="{{ 'password.request.submit'|trans }}" />
  {{ form_end(form) }}
{% endblock %}